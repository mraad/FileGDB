<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.esri</groupId>
    <artifactId>filegdb</artifactId>
    <version>${revision}</version>
    <packaging>jar</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <filegdb.version>0.68</filegdb.version>
        <revision>${filegdb.version}-${spark.compact}-${scala.compact}</revision>
        <!--
        Default build target. These live here rather than in an activeByDefault profile: Maven
        deactivates activeByDefault profiles as soon as any other profile auto-activates, which
        would silently break the build the moment the jdk9+ profile below kicks in. The spark-3.x
        profiles override them when selected explicitly with -Pspark-3.3 / -Pspark-3.4.
        -->
        <java.version>1.8</java.version>
        <scala.compact>2.12</scala.compact>
        <scala.version>${scala.compact}.21</scala.version>
        <scalatest.version>3.2.19</scalatest.version>
        <spark.compact>3.5</spark.compact>
        <spark.version>${spark.compact}.9</spark.version>
        <spark.scope>provided</spark.scope>
        <!-- Set by the jdk9+ profile; empty on JDK 8, which rejects the add-opens flag syntax. -->
        <test.argLine/>
    </properties>

    <name>${project.artifactId}_${scala.compact}</name>

    <dependencies>
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql_${scala.compact}</artifactId>
            <version>${spark.version}</version>
            <scope>${spark.scope}</scope>
        </dependency>
        <dependency>
            <groupId>org.scalatest</groupId>
            <artifactId>scalatest_${scala.compact}</artifactId>
            <version>${scalatest.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <executions>
                    <execution>
                        <!--
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <classifier>${scala.compact}</classifier>
                        </configuration>
                        -->
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>net.alchim31.maven</groupId>
                <artifactId>scala-maven-plugin</artifactId>
                <version>4.9.6</version>
                <executions>
                    <execution>
                        <id>scala-compile-first</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>scala-test-compile-first</id>
                        <phase>process-test-resources</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <scalaCompatVersion>${scala.compact}</scalaCompatVersion>
                    <scalaVersion>${scala.version}</scalaVersion>
                    <recompileMode>incremental</recompileMode>
                    <!--
                    <useZincServer>false</useZincServer>
                    -->
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.scalatest</groupId>
                <artifactId>scalatest-maven-plugin</artifactId>
                <version>2.2.0</version>
                <configuration>
                    <reportsDirectory>${project.build.directory}/surefire-reports</reportsDirectory>
                    <junitxml>.</junitxml>
                    <filereports>TestSuite.txt</filereports>
                    <argLine>${test.argLine}</argLine>
                    <!--
                    Bind the driver to loopback. Spark 4.0 gives each session an artifact
                    class loader and fetches unknown classes over RPC from the driver's own
                    address; Janino probes <generated package>.Object on every codegen
                    compile, so a host that refuses the self-connection on its LAN address
                    (the macOS firewall does) turns each probe into a RemoteClassLoaderError
                    and kills the task. Spark 3.x never used that loader, so it did not care.
                    -->
                    <systemProperties>
                        <spark.driver.bindAddress>127.0.0.1</spark.driver.bindAddress>
                        <spark.driver.host>127.0.0.1</spark.driver.host>
                    </systemProperties>
                </configuration>
                <executions>
                    <execution>
                        <id>test</id>
                        <goals>
                            <goal>test</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
        <profile>
            <id>spark-1.6</id>
            <properties>
                <java.version>1.7</java.version>
                <scala.compact>2.10</scala.compact>
                <scala.version>${scala.compact}.7</scala.version>
                <scalatest.version>3.1.0</scalatest.version>
                <spark.version>1.6.2</spark.version>
                <spark.scope>provided</spark.scope>
            </properties>
        </profile>
        <profile>
            <id>spark-2.3</id>
            <properties>
                <java.version>1.8</java.version>
                <scala.compact>2.11</scala.compact>
                <scala.version>${scala.compact}.8</scala.version>
                <scalatest.version>3.1.0</scalatest.version>
                <spark.version>2.3.4</spark.version>
                <spark.scope>provided</spark.scope>
            </properties>
        </profile>
        <profile>
            <id>spark-2.4</id>
            <properties>
                <java.version>1.8</java.version>
                <scala.compact>2.11</scala.compact>
                <scala.version>${scala.compact}.12</scala.version>
                <scalatest.version>3.1.0</scalatest.version>
                <spark.version>2.4.8</spark.version>
                <spark.scope>provided</spark.scope>
            </properties>
        </profile>
        <profile>
            <id>spark-3.0</id>
            <properties>
                <java.version>1.8</java.version>
                <scala.compact>2.12</scala.compact>
                <scala.version>${scala.compact}.10</scala.version>
                <scalatest.version>3.1.0</scalatest.version>
                <spark.version>3.0.1</spark.version>
                <spark.scope>provided</spark.scope>
            </properties>
        </profile>
        <profile>
            <id>spark-3.1</id>
            <properties>
                <java.version>1.8</java.version>
                <scala.compact>2.12</scala.compact>
                <scala.version>${scala.compact}.10</scala.version>
                <scalatest.version>3.1.0</scalatest.version>
                <spark.compact>3.1</spark.compact>
                <spark.version>${spark.compact}.3</spark.version>
                <spark.scope>provided</spark.scope>
            </properties>
        </profile>
        <profile>
            <id>spark-3.2</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <java.version>1.8</java.version>
                <scala.compact>2.12</scala.compact>
                <scala.version>${scala.compact}.15</scala.version>
                <scalatest.version>3.2.11</scalatest.version>
                <spark.compact>3.2</spark.compact>
                <spark.version>${spark.compact}.2</spark.version>
                <spark.scope>provided</spark.scope>
            </properties>
        </profile>
        -->
        <profile>
            <id>spark-3.3</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <java.version>1.8</java.version>
                <scala.compact>2.12</scala.compact>
                <scala.version>${scala.compact}.15</scala.version>
                <scalatest.version>3.2.11</scalatest.version>
                <spark.compact>3.3</spark.compact>
                <spark.version>${spark.compact}.1</spark.version>
                <spark.scope>provided</spark.scope>
            </properties>
        </profile>
        <profile>
            <id>spark-3.4</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <java.version>1.8</java.version>
                <scala.compact>2.12</scala.compact>
                <scala.version>${scala.compact}.17</scala.version>
                <scalatest.version>3.2.16</scalatest.version>
                <spark.compact>3.4</spark.compact>
                <spark.version>${spark.compact}.0</spark.version>
                <spark.scope>provided</spark.scope>
            </properties>
        </profile>
        <profile>
            <id>spark-4.0</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <java.version>17</java.version>
                <scala.compact>2.13</scala.compact>
                <scala.version>${scala.compact}.16</scala.version>
                <scalatest.version>3.2.19</scalatest.version>
                <spark.compact>4.0</spark.compact>
                <spark.version>${spark.compact}.0</spark.version>
                <spark.scope>provided</spark.scope>
            </properties>
        </profile>
        <!-- spark-3.5 is the default; its properties live in the top-level <properties> block. -->
        <profile>
            <!-- Spark reaches into JDK internals - without these the test JVM dies at startup. -->
            <id>jdk9+</id>
            <activation>
                <jdk>[9,)</jdk>
            </activation>
            <properties>
                <test.argLine>
                    --add-opens=java.base/java.lang=ALL-UNNAMED
                    --add-opens=java.base/java.lang.invoke=ALL-UNNAMED
                    --add-opens=java.base/java.io=ALL-UNNAMED
                    --add-opens=java.base/java.net=ALL-UNNAMED
                    --add-opens=java.base/java.nio=ALL-UNNAMED
                    --add-opens=java.base/java.util=ALL-UNNAMED
                    --add-opens=java.base/java.util.concurrent=ALL-UNNAMED
                    --add-opens=java.base/java.util.concurrent.atomic=ALL-UNNAMED
                    --add-opens=java.base/sun.nio.ch=ALL-UNNAMED
                    --add-opens=java.base/sun.nio.cs=ALL-UNNAMED
                    --add-opens=java.base/sun.security.action=ALL-UNNAMED
                    --add-opens=java.base/sun.util.calendar=ALL-UNNAMED
                </test.argLine>
            </properties>
        </profile>
        <!--profile>
            <id>spark-3.2-test</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <java.version>1.8</java.version>
                <scala.compact>2.12</scala.compact>
                <scala.version>${scala.compact}.15</scala.version>
                <scalatest.version>3.2.11</scalatest.version>
                <spark.compact>3.2</spark.compact>
                <spark.version>${spark.compact}.1</spark.version>
                <spark.scope>compile</spark.scope>
            </properties>
        </profile-->
    </profiles>
</project>
